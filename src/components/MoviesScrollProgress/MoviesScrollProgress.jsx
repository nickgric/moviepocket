import { MoviesScrollProgressStyled } from "./MoviesScrollProgressStyled";

export const MoviesScrollProgress = ({ value, max }) => {
  return <MoviesScrollProgressStyled value={value} max={max} />;
};
